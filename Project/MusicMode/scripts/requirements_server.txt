# MusicMode 服务器端依赖（AutoDL RTX 5090 / Python 3.12 / CUDA 12.8）
# 生成日期：2026-04-15
# 使用方式：pip install -r requirements_server.txt
#
# 注意：torch==2.8.0 已由镜像内置，此处不重复安装以防降级
# =====================================================================

# ── 核心数值计算 ──────────────────────────────────────────────────────
# numpy < 2.0：implicit==0.7.2 对 numpy 2.x 不兼容，必须锁版本
numpy<2.0

pandas>=2.0.0,<3.0.0
scipy>=1.11.0

# ── 机器学习 ──────────────────────────────────────────────────────────
scikit-learn>=1.3.0,<2.0.0

# lightgbm 4.3.0：early_stopping API 在 4.4+ 有破坏性变更，锁定此版本
lightgbm==4.3.0

# ── 协同过滤（ALS）────────────────────────────────────────────────────
# implicit 0.7.2：最后一个对 numpy 1.x 完全兼容的稳定版本
implicit==0.7.2

# ── 向量检索（FAISS）─────────────────────────────────────────────────
# RTX 5090（Blackwell sm_120）架构过新，faiss-gpu 无预编译 CUDA 12.8 轮子
# 改用 faiss-cpu：API 完全相同，离线建索引场景性能足够
faiss-cpu

# ── 数据库 & 缓存 ────────────────────────────────────────────────────
pymysql>=1.1.0
redis>=5.0.0

# ── 工具类 ───────────────────────────────────────────────────────────
tqdm>=4.65.0
matplotlib>=3.7.0
