# MusicWeb 项目大手术修改计划（基于 AutoDL RTX 5090 32GB）

> 计划生成日期：2026-04-15
> 计划执行目标：答辩后项目代码大手术，解决评委反馈与笔记遗留问题
> 目标服务器：AutoDL 北京B区 766 机 / RTX 5090 32GB 单卡 / 25 核 Xeon 8470Q / 90GB RAM
> 目标镜像：PyTorch 2.8.0 / Python 3.12 / Ubuntu 22.04 / CUDA 12.8

---

## 一、总览

本次修改是一次"从预处理到评估"的全链路手术，需严格按代码执行顺序推进，避免破坏上下游依赖。工作流分为五大板块：

1. 项目结构迁移（Project + Mode 双端重组）
2. 环境对齐与 Smoke Test（含小量数据时间预测）
3. 离线全模型训练（按 10 步顺序执行）
4. 批量推荐生成管道验证
5. 论文章节同步更新（本计划暂不展开论文修改）

执行前提：每一阶段完成后须经 Master 核验通过，方可进入下一阶段。

---

## 二、AutoDL 服务器与环境对齐

### 2.1 服务器配置确认

| 项 | 配置 |
|---|---|
| GPU | RTX 5090 32GB × 1（Blackwell 架构 sm_120） |
| CPU | Xeon Platinum 8470Q 25 核 |
| 内存 | 90GB |
| 数据盘 | 50GB 免费 + 扩容至 50GB（共 100GB），不足再扩 |
| 系统盘 | 30GB |
| GPU 驱动 | 580.105.08 |
| CUDA 运行时 | 12.8（镜像内置） |
| 基础镜像 | PyTorch 2.8.0 / Python 3.12 / Ubuntu 22.04 |

### 2.2 PyTorch 镜像适配性审查

所选镜像 PyTorch 2.8.0 + CUDA 12.8 是 RTX 5090 的最稳组合，原因如下：

- Blackwell 架构必须 PyTorch 2.7+ 才能识别 sm_120
- CUDA 12.8 是 5090 支持的最低运行时版本
- Python 3.12 与主流科学计算库（numpy、pandas、sklearn、lightgbm）适配成熟
- Ubuntu 22.04 是 LTS 稳定版，implicit/faiss-gpu 均有官方轮子

### 2.3 本地到服务器的兼容性风险清单

| 风险项 | 处理方案 |
|---|---|
| NumPy 1.x → 2.x breaking change | requirements 锁定 numpy<2.0 |
| LightGBM 4.x early_stopping API 变更 | 锁定 lightgbm==4.3.0 |
| implicit 对 numpy 2.x 敏感 | 锁定 implicit==0.7.2 |
| faiss 本地 CPU → 服务器 GPU | 服务器装 faiss-gpu==1.8.0 |
| Windows GBK 编码 vs Linux UTF-8 | 全项目 grep `open(` 未显式带 `encoding='utf-8'` 的补全 |
| Windows 反斜杠路径 `F:\\xxx` | 全项目 grep 硬编码路径改 `os.path.join` 或 `pathlib` |
| pickle 跨 Python 版本 | 本地 Py3.x 存的 .pkl 在服务器 Py3.12 先做读取验证 |
| torch.cuda.amp.autocast 新版 API | 统一改用 `torch.amp.autocast('cuda', ...)` |

### 2.4 环境对齐执行步骤（第 0 步，服务器开机后立即执行）

1. 本地执行 `pip freeze > requirements_local.txt`，拉取所有依赖精确版本
2. 服务器创建 `requirements_server.txt`，按 2.3 表锁定关键包
3. 执行 `pip install -r requirements_server.txt`
4. 编写 `tools/scan_encoding.py` 一次性脚本，扫描所有 `.py` 文件中 `open(` 调用未带 `encoding='utf-8'` 的，批量补全
5. 编写 `tools/scan_paths.py` 一次性脚本，扫描所有硬编码 Windows 路径，替换为 `pathlib.Path`
6. 两个一次性脚本执行完后立即删除（符合 CLAUDE.md 第 8 条）
7. 执行 `python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name())"` 验证 5090 可用

---

## 三、Smoke Test 与时间预测（第 0.5 步，训练前必做）

### 3.1 目的

在正式训练前用 1% 采样数据跑通完整 pipeline，达成两个目标：

- 验证代码在服务器环境下无运行时错误（import/编码/路径/API 兼容）
- 基于 1% 数据训练耗时线性外推出全量训练时间，决定是否调整方案

### 3.2 执行内容

1. 从 features_v3.pkl 随机采样 1% 的用户作为 smoke 子集
2. 依次跑通：数据清洗 → 特征工程 → ALS 粗扫（仅 1 组参数）→ DeepFM 1 epoch → BST 1 epoch → LightGBM → FAISS → MMR → 批量推荐
3. 记录每一步耗时，写入 `smoke_test_report.md`
4. 外推全量训练时间，与预算对比
5. 外推时间若超出预期 1.5 倍，暂停训练并与 Master 讨论调整

### 3.3 预估时间

Smoke Test 总耗时约 30-40min。

---

## 四、项目结构迁移（第 1 步）

### 4.1 Project 文件夹重组

原结构为 14 个扁平 .py 文件，迁移后按推荐漏斗层级分 8 个子文件夹（命名采用方案二，英文）：

```
Project/MusicMode/Project/
├── data_cleaning/
│   └── data_cleaning.py
├── feature_engineering/
│   ├── prepare_features_v3.py
│   └── compute_recall_scores.py（待抽出）
├── recall/
│   ├── build_faiss_index.py
│   └── train_als.py
├── coarse_rank/
│   └── train_lgbm.py
├── fine_rank/
│   ├── train_deepfm_v3.py
│   ├── train_bst.py
│   └── build_ensemble.py
├── rerank/
│   └── mmr_rerank.py（待抽出独立模块）
├── evaluation/
│   ├── eval_experiment.py
│   ├── evaluate_offline.py
│   ├── evaluate_recs.py
│   └── generate_auc_chart.py
└── serving/
    ├── refresh_song_stats.py
    └── sync_recs_v3.py
```

### 4.2 Mode 文件夹重组

原 Mode/ 下 9 个公共 .pkl 与 4 个子文件夹并列，迁移后按归属分层：

```
Project/MusicMode/Mode/
├── feature_engineering/
│   ├── encoders_v3.pkl
│   ├── features_v3.pkl
│   ├── features_v3_cache.npz
│   ├── features_seq.pkl
│   ├── song_stats.pkl
│   ├── user_stats.pkl
│   └── svd_vecs.pkl
├── recall/
│   ├── song_index.faiss
│   ├── song_id_map.pkl
│   ├── als_model.pkl
│   └── candidates.pkl
├── coarse_rank/
│   └── lgbm/（原子目录）
├── fine_rank/
│   ├── deepfm/
│   ├── bst/
│   └── ensemble/
└── evaluation/
    ├── evaluation_report.txt
    └── offline_evaluation_report.txt
```

### 4.3 迁移执行注意

- 所有引用这些文件路径的 .py 脚本须同步修改常量路径
- 使用 `git mv` 保留文件历史
- 迁移后执行一次 Smoke Test 确认所有脚本可跑通
- 迁移操作必须经 Master 核验后才能执行

---

## 五、10 步训练执行顺序（第 2-11 步）

以下顺序严格按代码依赖关系排定，任何步骤不得跳过或换序：

| 步 | 脚本 | 依赖输入 | 产出 | 单次耗时（5090 32GB） |
|---|---|---|---|---|
| 2 | data_cleaning.py | 原始 KKBOX 数据 | 清洗后交互表 | 15-20min |
| 3 | prepare_features_v3.py | 清洗表 | features_v3.pkl / encoders_v3.pkl / features_seq.pkl | 25-35min |
| 4 | refresh_song_stats.py | features_v3.pkl | song_stats.pkl / user_stats.pkl / svd_vecs.pkl | 10-15min |
| 5 | train_als.py（四维网格粗+精） | features_v3.pkl | als_model.pkl / candidates.pkl | 4-5h |
| 6 | train_deepfm_v3.py（5 折联机） | features_v3.pkl | deepfm/*.pth × 5 fold | 3-5h |
| 7 | build_faiss_index.py | deepfm/deepfm_model.pth | song_index.faiss / song_id_map.pkl | 20-30min |
| 8 | compute_recall_scores.py（新抽出） | FAISS+ALS+热度 | recall_scores.pkl | 40min-1h |
| 9 | train_bst.py（5 折联机） | features_seq.pkl | bst/*.pth × 5 fold | 4-6h |
| 10 | train_lgbm.py（含 Schema 映射） | features_v3.pkl + recall_scores | lgbm/*.pkl | 30-45min |
| 11 | build_ensemble.py | deepfm + bst + lgbm | ensemble/*.pkl | 15-20min |

**总训练时间估计**：约 14-20h（可跨夜执行，白天分析结果）

**Point-in-Time 方案 C 融入说明**：第 5/6/9/10 步内部均按"特征观察期 80% + 标签预测期 20%"切分，训练集仅用观察期数据构造，标签只在预测期打。

---

## 六、按笔记溯源的修复任务清单

### 6.1 召回层（Recall Layer）

#### 任务 R1：FAISS ① 维度坍塌冷热分流

- **笔记来源**：FAISS向量检索原理（通道A）五、问题1
- **修复难度**：中
- **修复前影响**：冷门歌曲因 DeepFM embedding 未充分训练而语义坍塌，FAISS 返回同质化结果
- **修复后影响**：冷门歌曲走 SVD 分支，热门走 DeepFM 分支，召回多样性提升
- **代码改动**：build_faiss_index.py 新增 `is_in_svd` 显式标记特征，Graceful Degradation 冷热分流逻辑
- **预计执行时间**：2-3h

#### 任务 R2：ALS Feature Pass-Through（特征透传）

- **笔记来源**：ALS 协同过滤原理（通道C）五、问题3
- **修复难度**：低
- **修复前影响**：Min-Max 归一化后三路召回分数相加存在三个致命漏洞（未激活样本被错误惩罚、量纲压缩、零值歧义）
- **修复后影响**：原始分数透传，LightGBM 自行学习分数权重
- **代码改动**：compute_recall_scores.py（新抽出）中 `faiss_score` / `als_score` / `hot_score` 三列直接透传
- **预计执行时间**：1-2h

#### 任务 R3：ALS 四维网格搜索（粗+精两阶段）

- **笔记来源**：ALS 协同过滤原理（通道C）五、问题4
- **修复难度**：中
- **修复前影响**：单一 rank=50 未经调优，召回 Recall@K 可能偏低
- **修复后影响**：在 192 组合空间中通过粗+精两阶段高效定位最优超参
- **参数网格**：
  - 粗扫（24 组）：rank {32, 128, 256} × reg {0.01, 0.1} × alpha {1, 40} × iter {20}
  - 精扫（12 组）：围绕粗扫最优点 ±1 档微调
- **预计执行时间**：4-5h（CPU 并行）

#### 任务 R4：热度召回五连环缺陷修复

- **笔记来源**：热度召回原理（通道B）五、问题1-5
- **修复难度**：中
- **修复前影响**：完播率假惩罚、马太效应、加法 artist_bonus 崩溃等五处原理性错误
- **修复后影响**：
  - 负权重斥力向量（修复完播率假惩罚）
  - 对数动量分（抑制马太效应）
  - 乘法式 artist_bonus（替代加法崩溃）
- **代码改动**：refresh_song_stats.py 与 compute_recall_scores.py
- **预计执行时间**：3-4h

### 6.2 粗排层（Coarse Rank Layer）

#### 任务 C1：LightGBM 特征名 Schema 映射

- **笔记来源**：LightGBM 粗排原理与工程架构优化 七、问题1
- **修复难度**：中
- **修复前影响**：硬编码列索引，特征顺序一旦变动即全盘错位
- **修复后影响**：基于特征名的 Schema 映射，列顺序解耦
- **预计执行时间**：2-3h

#### 任务 C2：LightGBM 超参优化

- **笔记来源**：LightGBM 粗排原理与工程架构优化 七、问题2
- **修复难度**：低
- **修复前影响**：num_leaves 与 max_depth 参数冲突导致树被截断
- **修复后影响**：num_leaves=160 + max_depth=8 协调配置
- **预计执行时间**：30min（含重训）

#### 任务 C3：Redis Feature Store 分层预计算（与 LightGBM 改造同步）

- **笔记来源**：LightGBM 粗排原理与工程架构优化 七、问题3
- **修复难度**：高
- **修复前影响**：粗排阶段特征 I/O 是线上瓶颈
- **修复后影响**：热 Key 预计算驻留 Redis，冷 Key 走 DB 回源
- **执行方式**：LightGBM 改造完成后立即压力测试
- **预计执行时间**：6-8h（含压测）

### 6.3 精排集成层（Fine Rank Layer）

#### 任务 F1：DeepFM Padding/UNK 编码偏移 +2

- **笔记来源**：DeepFM 模型原理与实现 六、6.1
- **修复难度**：低
- **修复前影响**：0 既做 Padding 又可能代表真实值，embedding 训练污染
- **修复后影响**：0=Padding、1=UNK、真实值从 2 起
- **预计执行时间**：1-2h

#### 任务 F2：DeepFM 5 折联机集成

- **笔记来源**：DeepFM 模型原理与实现 六、6.2
- **修复难度**：中
- **修复前影响**：单体部署与训练分布漂移
- **修复后影响**：5 折联机预测，评估指标稳定性提升
- **预计执行时间**：3-5h（服务器串行 5 次训练）

#### 任务 F3：BST Padding/UNK 编码偏移 +2

- **笔记来源**：BST 模型原理与实现 六、6.1
- **修复难度**：低
- **修复前影响**：同 F1
- **修复后影响**：同 F1
- **预计执行时间**：1-2h

#### 任务 F4：BST 新歌 UNK 占位

- **笔记来源**：BST 模型原理与实现 六、6.2
- **修复难度**：低
- **修复前影响**：新歌无历史序列导致 BST 无法打分
- **修复后影响**：UNK 占位使新歌可进入 BST 评估
- **预计执行时间**：1-2h

#### 任务 F5：BST 序列长度扩展

- **笔记来源**：BST 模型原理与实现（容量扩展讨论）
- **修复难度**：低
- **修复前影响**：max_len=20 可能截断长期偏好
- **修复后影响**：max_len=50 主方案 + max_len=100 附录对比
- **其它参数**：Transformer 层数保持代码当前值 3，attention heads 从 4 扩到 8
- **预计执行时间**：4-6h（5 折串行训练）

#### 任务 F6：DeepFM / BST 模型容量扩展

- **笔记来源**：模型容量升级讨论
- **修复难度**：低
- **修复前影响**：本地显存限制下 embedding_dim=16 偏小
- **修复后影响**：embedding_dim=32 主方案 + 64 对比实验，DeepFM MLP [256,128,64]，batch_size 2048
- **预计执行时间**：包含在 F2/F5 训练时间内

### 6.4 重排层（Rerank Layer）

#### 任务 RR1：MMR 新歌劫榜修复

- **笔记来源**：MRR 原理与实现 五、5.3
- **修复难度**：中
- **修复前影响**：新歌因相似度低被 MMR 错误偏好，造成劫榜
- **修复后影响**：分层兜底相似度（0.75 同艺/0.40 同流派/0.15 全异）
- **预计执行时间**：2-3h

#### 任务 RR2：MMR NumPy 矩阵化

- **笔记来源**：MRR 原理与实现 六、6.2
- **修复难度**：低
- **修复前影响**：Python 循环计算相似度矩阵，单用户推荐耗时 200ms+
- **修复后影响**：C_normed @ S.T 一次性完成，单用户降至 5ms
- **预计执行时间**：1-2h

### 6.5 系统评估与展望

#### 任务 E1：评估体系重构

- **笔记来源**：第六章评估体系重构与展望设计 二、三、四
- **修复难度**：中
- **修复前影响**：评估指标覆盖不全，冷启动分层未单独报告
- **修复后影响**：
  - 冷启动四档分层评估（1-9 / 10-49 / 50-199 / ≥200 次交互）
  - Most Popular 基准沙袋柱加入 AUC 对比图
- **预计执行时间**：3-4h

#### 任务 E2：第七章展望（仅文档，不动代码）

- **笔记来源**：第六章评估体系重构与展望设计 五
- **处理方式**：论文第七章如实表述 ε-贪心 + IPS 逆倾向加权为未来工作，本轮不实现

### 6.6 算力无关搁置项（论文如实表述，本轮不动代码）

| 问题 | 搁置理由 |
|---|---|
| FAISS ② 信息茧房 | 架构级限制，召回层引入 ε-贪心会被精排+重排过滤，仅在 Mode/rerank/mmr_lambda_sweep 实验阶段做对照观察，不入主流程 |
| FAISS ③ BST 不参与 FAISS | 架构级限制，BST 只服务于精排集成层 |
| FAISS ④ 论文表述瑕疵 | 论文阶段直接改写 |

---

## 七、严格 Point-in-Time 切分方案（双方案并列，供导师审阅）

### 7.1 方案 A：时序快照滚动

- **做法**：按月切分 2016-09 至 2017-04 共 8 个月，第 k 个月用 [起点, 月 k-1] 训练、在月 k 上评估
- **侧重点**：工业部署真实性，模拟线上系统"每月重训一次模型"的服役场景
- **适合场景**：给出模型在时间轴上的稳定性曲线，回答"系统实际上线后指标如何漂移"
- **训练次数**：8 次
- **叠加 5 折集成**：8 × 5 = 40 次（不建议，训练爆炸）
- **服务器训练耗时**：若单独执行约 6-10h

### 7.2 方案 C：特征观察期 + 标签预测期

- **做法**：一次性切分，前 80% 时间窗口作为"特征观察期"（只用这段构造用户/物品历史特征），后 20% 为"标签预测期"（只在这段打 label、算指标）
- **侧重点**：学术严谨性 + 工程可行性的平衡，彻底杜绝 Data Leakage 的同时保持单次训练的轻量级
- **适合场景**：本科毕设离线实验，证明方法在严格时序约束下仍有效
- **训练次数**：1 次
- **叠加 5 折集成**：1 × 5 = 5 次（可行）
- **服务器训练耗时**：不增加额外开销，融入主训练流程

### 7.3 方案并列对比表

| 维度 | 方案 A 时序快照滚动 | 方案 C 观察期+预测期 |
|---|---|---|
| 核心目的 | 工业部署真实性 | 学术严谨性 |
| 训练次数 | 8 次 | 1 次 |
| 与 5 折集成叠加 | 40 次（不可行） | 5 次（可行） |
| 产出指标 | 时间稳定性曲线 | 单点严格指标 |
| 适合场景 | 模拟线上持续服役 | 本科毕设离线实验 |
| 额外训练时间 | +6-10h | 0 |
| 消除 Data Leakage | 彻底 | 彻底 |

### 7.4 推荐方案

主方案采用方案 C，方案 A 作为未来工作或附录扩展实验。最终选择由导师审阅后定夺。

---

## 八、完整执行时间预算

| 阶段 | 子任务 | 耗时 |
|---|---|---|
| 第 0 步 | 环境对齐 | 1-2h |
| 第 0.5 步 | Smoke Test（1% 采样） | 30-40min |
| 第 1 步 | 项目结构迁移 | 2-3h |
| 第 2-4 步 | 数据清洗 + 特征工程 + 统计刷新 | 1-1.5h |
| 第 5 步 | ALS 网格搜索（粗+精） | 4-5h |
| 第 6 步 | DeepFM 5 折联机 | 3-5h |
| 第 7 步 | FAISS 建索引 | 20-30min |
| 第 8 步 | 多路召回分数计算 | 40min-1h |
| 第 9 步 | BST 5 折联机 | 4-6h |
| 第 10 步 | LightGBM 训练 | 30-45min |
| 第 11 步 | Ensemble 构建 | 15-20min |
| 第 12 步 | 批量推荐管道验证 | 1-2h |
| 第 13 步 | 评估体系重构 | 3-4h |
| 第 14 步 | Redis Feature Store 压测 | 6-8h |
| **合计** | | **约 28-40h** |

预算按两天跨夜训练安排，白天做代码审查与结果分析。

---

## 九、风险与应急预案

1. Smoke Test 失败：暂停训练，定位环境兼容性问题，与 Master 讨论
2. 外推时间超预算 1.5 倍：暂停训练，评估是否降维（ALS 网格降到仅粗扫、BST 放弃 100 对比实验）
3. 显存溢出：batch_size 降半、启用梯度累积、启用 FP16 AMP
4. 服务器中断：所有 .pkl 与 .pth 产出必须在每步结束后立即备份到数据盘持久化目录
5. 论文数据一致性：所有训练产出以此次执行结果为准，论文旧数值全部刷新

---

## 十、Master 审阅确认

本计划覆盖以下已批准项：

- 项目结构迁移（方案二英文命名，8 个子文件夹）
- 环境对齐第 0 步（requirements 锁版本、UTF-8 编码、路径统一）
- Smoke Test 时间预测
- FAISS ① 冷热分流、R2 Feature Pass-Through、R3 四维网格（粗+精两阶段）
- 热度召回五连环缺陷修复
- LightGBM Schema 映射 + 超参优化 + Redis Feature Store
- DeepFM / BST Padding+UNK 偏移 +2
- 5 折联机集成（DeepFM + BST）
- BST 序列扩展 max_len=50 主 / 100 附录，Transformer 层数保持代码当前值 3，heads=8
- DeepFM/BST 容量扩展 embedding_dim=32 主 / 64 对比
- MMR 新歌劫榜分层兜底 + NumPy 矩阵化
- 评估体系重构（冷启动四档分层 + Most Popular 基准）
- Point-in-Time 方案 A 与方案 C 并列写入（供导师审阅，主推 C）

本计划明确搁置以下项（论文如实表述）：

- FAISS ② 信息茧房（仅 mmr_lambda_sweep 实验观察）
- FAISS ③ BST 不参与 FAISS（架构级限制）
- FAISS ④ 论文表述瑕疵（论文阶段改写）
- 第七章展望 ε-贪心 + IPS（未来工作）

待 Master 最终确认后进入执行阶段。
