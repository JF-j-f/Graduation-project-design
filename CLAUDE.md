**MusicWeb 项目要求**

（这是一个在线音乐平台开发项目，该项目的目标是实现一个高精度的个性化音乐推荐系统）

<INSTRUCTIONS>
1. 称呼我为主人。

1. 语言要求：所有回复、思考过程及任务清单，均须使用中文与我交流。
2. 我的电脑配置是机械革命旷世16 pro（内存为16g），系统是Windows11专业版。
3. GPU：NVIDIA GeForce RTX 4060 Laptop (8GB VRAM)
4. CUDA：13.1 驱动:591.74
5. 在执行命令前，你必需去阅读E:\Graduation-project-design\Project\.agent\workflows路径下的所有文档！其中musicweb文档是你在进行代码开发、修复等与代码有关的场景下所必需遵守的指令；而update-markdown文档是我说总结今日工作内容时，你要遵守的指令。
6. 该项目已经拥有了项目说明文档Readme.md和更新日志文档CHANGELOG.md，你都需要进行了解！
7. 所有产生的一次性文件，都必须在完成任务后删除！并且生成的文件应该要放在当前工作目录的对应位置。

---

**论文写作专项规范（每次生成论文内容前必须遵守）**

8. 【修改粒度规则】

   - 原文整节存在结构性或方向性问题 → 重写整节
   - 原文单一段落有问题 → 只重写该段落，不动其他段落
   - 原文单一句子有问题 → 只重写该句，不动其他内容
   - 禁止因局部问题大范围改动无关内容
9. 【工作流程规范】

   - 每次修改章节内容前，必须先提交大纲供主人审核，审核通过后才能开始生成正文
   - 每次生成修改大纲前，必须先查看对应章节涉及的项目代码实现逻辑，生成的大纲内容须基于代码实际，不得夸大描述、虚假构造或凭印象臆写
   - 修改前必须先读对应章节原文及相关代码文件，不得凭印象直接动笔
   - 每章修改完成后，必须更新 Document/论文审阅意见（V4）.md，标注进度与新发现错误
10. 【引言写法规则】

    - 每章/每节引言必须承上启下：向上简短呼应上一章/节核心结论，向下引出本章/节展开内容
    - 不得写成目录式列举，不得完全不提上章
11. 【禁用词语清单】——以下词语及同类绝对化表述，严禁出现在论文正文中：

    - 从根本上、彻底、完全杜绝、根本性地、根本上防范、从根本上消除
    - 完美、极致、最优秀、无与伦比、史无前例
    - 大幅提升、显著改善（无数据支撑时禁用）
    - 任何语气过于绝对的副词或形容词
12. 【中英文与数字排版规则】——严禁在以下情况添加空格：

    - 中文与英文之间不加空格（如：基于Java构建，不是：基于 Java 构建）
    - 中文与数字之间不加空格（如：共59维，不是：共 59 维）
    - 中文与括号之间不加空格
    - 中文与标点之间不加空格
    - 注意：英文单词与英文单词之间、英文与英文括号之间保持正常英文空格规则
13. 【图片生成提示词规则】（给Gemini生成图片时使用）：

    - 必须用英文编写提示词
    - 精确列出图中所有模块、组件名称、箭头方向与标注文字
    - 指定风格为 clean technical architecture diagram，白色背景
    - 图内层与层之间必须明确指定箭头及方向
    - 图内文字：中文用宋体、英文和数字用Times New Roman，字号统一5号字
    - 不得在中文层名后添加括号注释（如：数据访问层（DAO）→只写数据访问层）
    - 所有模块名称、数值必须在提示词中明确写出，不让Gemini自行发挥
14. 【特征维度括号注释规则】：

    - 每个维度的详细分项说明（如：59维（7个稀疏特征与52个稠密特征））在全文中仅出现一次
    - 后续章节再次提及该维度时，只写数字（如：59维），不重复括号内容
15. 【章节间引用规则】：

    - 前章节已出现的公式、图片，在之后的章节中应直接引用（如：见式(2-8)、见图2-4）
    - 只有前章节完全未提及的新公式或新图片，才在之后的章节中新增
16. 【正文排版格式规范】：

    - 严禁使用斜体（不得用 *文字* 或 _文字_ Markdown格式）
    - Word文档正文格式：宋体小四号，多倍行距值1.25，段前0行，段后0行，首行缩进2字符
    - 输出正文时不得携带任何Markdown格式符号
17. 【字数控制规则】：

    - 全文目标：32000–35000字；前三章已有约13900字
    - 第四章+第五章合计须控制在20000字以内
    - 数据库11张表的详细结构图表全部移至附录1，不占正文空间
18. 【公式注释格式规范】：

    - 每个公式下方必须紧跟"式中"注释块，不得省略
    - 格式：首行写"式中"后空4个半角字符，再写第一个变量；后续变量另起一行，与破折号后第一个字对齐
    - 每个变量用破折号"——"（占4个半角字符）连接说明文字，末尾用分号"；"，最后一项用句号"。"
    - 若变量的说明需用公式表示，该说明公式后不加公式序号
    - 公式中物理量符号用斜体，计量单位与说明文字用正体
    - 生成LaTeX代码时：函数名（如MMR、Rel、Sim、max）用\mathrm{}包裹，变量用默认斜体，下标用_{}表示

20. 【第五章公式后实现应用句规范】（仅适用于第五章）：

    - 式中注释块结束后，必须紧跟一句"实现应用句"，说明该公式在系统代码中如何被调用或使用
    - 实现应用句格式：以"在[模块/脚本名]实现中，……"或"系统在[具体场景]中调用该指标……"开头
    - 禁止将实现应用句写成原理解释（不得说"该公式揭示了……""其本质是……"）
    - 正确示例："在prepare_features_v3.py的特征构建阶段，系统对每首候选歌曲计算trend_ratio，并将其作为稠密特征直接输入模型，用于区分近期热度上升与衰退的歌曲。"
    - 错误示例："该指标大于1时说明歌曲近期热度加速上升，小于1时代表热度趋于衰退。"（此为原理解释，不是实现说明）
19. 【第五章关键参数速查（写第五章内容时必须以此为准，严禁凭印象填写）】：

    **推荐管道漏斗参数（sync_recs_v3.py）**：

    - RECALL_FAISS=200, RECALL_ALS=200, RECALL_HOT=200 → 三路合并后约600首
    - RANK_TOP=300（粗排保留），ENSEMBLE_TOP=150（精排保留），TOP_N=50（最终输出）
    - MMR_LAMBDA=0.7，MAX_PER_ARTIST=10（安全上限）

    **FAISS参数（build_faiss_index.py）**：

    - 向量维度：160维（5类稀疏特征×32维：song_id+genre+language+artist+origin_country）
    - 索引类型：IndexFlatIP + L2归一化 → 等价余弦相似度，精确搜索非近似

    **热度通道实际排序逻辑（sync_recs_v3.py）**：

    - 主排序：hot_cache（total_plays预排的前10000首）+ ARTIST_BONUS_MAP偏好艺术家加权分
    - 加权分：dissatisfied=800 / neutral=500 / satisfied=300 / very_satisfied=150
    - 无发行年份参与排序！
    - 回退方案（hot_cache失败时）：按songs.popularity降序

    **模型超参数查阅规则（严格执行）**：

    - 凡需引用LightGBM、DeepFM、BST任一模型的超参数，必须直接查阅对应训练脚本（train_lgbm.py、train_deepfm_v3.py、train_bst.py）中的实际变量定义，不得凭印象或任何历史记录填写
    - 历史记录随代码迭代极易过时，DeepFM参数曾因此造成论文6处错误，教训深刻

    **集成层（build_ensemble.py）**：

    - 参与集成的模型：仅DeepFM+BST两个精排模型，LightGBM不参与集成
    - 权重优化：SLSQP约束优化，目标=最小化负AUC，约束：w₁+w₂=1，各权重≥0

    **表5-4用户画像字段（历史版本有copy-paste错误，正确字段为）**：

    - msno(用户唯一标识)、city(城市编码)、bd(出生年份)、gender(性别)
    - registered_via(注册渠道)、registration_init_time(注册时间戳)、expiration_date(会员过期时间戳)
